namespace API_Data.src.Enum
{
    public enum TipoLancamento
    {
        Unico = 1,      // À vista ou gasto pontual
        Parcelado = 2,  // Com número fixo de parcelas
        Fixo = 3        // Recorrente mensal (ex: energia, internet)
    }
}
